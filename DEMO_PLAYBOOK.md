# Demo Playbook

## 演示目标

展示一个本地生活执行型 Agent：不是只回答“去哪玩”，而是把用户目标补清楚、给出可比较方案、收朋友反馈、完成 Mock 预约，并在执行页处理账单、加购和半路救援。

本项目全部为本地 Mock：不连接线上商家系统、不写入生产订单系统、不触发线上支付、不调用外部客服系统、不使用线上地图。

## 开场话术

“我们这轮把重点放在工作流，而不是继续堆小功能。用户输入一句话后，系统先确认哪些信息是真的由用户说过，哪些还缺。缺信息时不硬推；信息够了才生成 Plan A / Plan B，朋友确认后才预约。”

## 演示路线一：生日组局闭环

输入：

```text
朋友生日，4个人，预算300一人，想有点仪式感
```

应展示：

- 本次目标：朋友生日仪式感安排。
- 候选方案：活动、餐厅、生日送达节点。
- 朋友确认：候选页内可以生成 Mock 共选链接。
- 预约抽屉：最终确认前只显示待预约段，不生成 booking。
- 执行页：预约完成后出现账单、分账、顺手加购和救援。

讲解点：

- 生日场景的价值在于跨品类协调，而不是只推荐一个餐厅。
- 蛋糕/鲜花是生日送达节点，不应混进“路上加一杯”。
- Mock 预约和 Mock 账单只在最终确认后出现。

## 演示路线二：剧本杀缺信息

输入：

```text
我想玩个剧本杀
```

应展示：

- 系统停在“还差这些信息”。
- 必须追问人数、时间、预算、剧本类型、可用时长、区域。
- 不直接生成随机商户。

补齐后输入或选择：

```text
4个朋友今晚19:00想玩欢乐盒装本，人均150，新街口，公共交通，4小时
```

应展示：

- 剧本杀卡片展示本名、类型、人数、拼场状态、加入后是否可成行、DM 评分、新手友好、恐怖度和时长。
- 主方案只放剧本杀；餐饮和奶茶只能作为可选内容，不自动进入账单。

## 演示路线三：自然吃饭与宅家语义

到店吃饭输入：

```text
我和3个朋友今晚19:00想在新街口到店吃饭，人均150，想少排队，菜系都可以。
```

应展示：

- 不再因为 `dine_mode` 或 `cuisine_preference` 卡住。
- `dine_mode` 是到店吃的规划默认值，仍可修改。
- “菜系都可以”被识别为不限菜系，不继续追问菜系。

宅家输入：

```text
今晚宅家，2个人，预算100，想点外卖和零食，不想出门。
```

应展示：

- 主线是外卖正餐 + 闪购零食。
- 不追问商圈、到店方式或餐厅预约。
- 展示配送时间、配送费、美团外卖/闪购语义。

跨饭点活动输入：

```text
我想先玩剧本杀再打台球，19点开始，新街口，4个人，人均200，能玩5小时。
```

应展示：

- 主线顺序保持剧本杀 -> 台球。
- 因跨晚饭时间出现可选 meal bridge/中间补给提示。
- 饭点补给不是用户已确认主目标，不会自动进入预约。

## 演示路线四：朋友共选与预约顺序

在候选方案页：

1. 选择 Plan A 或 Plan B。
2. 点击“发给朋友确认”生成 Mock 共选链接。
3. 模拟朋友反馈“晚到半小时”或“餐厅担心排队”。
4. 如果反馈触发硬约束，先局部修改，再由发起人最终预约。
5. 打开底部预约抽屉，确认后才进入执行页。

验收点：

- 好友反馈不能出现在预约完成之后。
- 共选不会自动预约。
- 预约抽屉必须明确“不会联系真实商家”。

## 演示路线五：半路救援

在预约完成后的执行页点击：

```text
餐厅满座
```

应展示：

- 行程遇到问题，想修改？
- 只替换问题节点。
- 保留未受影响的其它节点。
- 如超预算或时间变化，必须提示用户确认。

讲解点：

- 这证明 Agent 不是生成方案就结束，而是能在执行过程中做半路救援。
- 这仍是本地 Mock，不代表真实排队或库存。

## 验收关注

- Intent Contract：只展示 explicit text / user answer / profile memory。
- Required Slots Contract：缺关键槽位就停下来问。
- Plan A/B：候选方案横向比较，而不是一条长日志。
- 多人投票：Mock 共选，不使用 WebSocket。
- Mock 边界：支付、客服、商家预约、地图、配送都不接真实服务。
- Natural Intent Coordination：到店吃默认、菜系不限、宅家配送、多活动跨饭点补给要互不污染。

## Phase 2C Browser/Deploy Polish Checks

- 候选卡应该像“店铺推荐卡”，不是字段表格：顶部看 Plan A/B、店名、品类，下面自然展示价格、时长、交通、评分、排队/预约、关键词和风险提示。
- Stepper 只能回到已完成步骤；未解锁步骤会提示“请先完成当前步骤”。
- 改补充信息、改候选方案、改预约信息后，下游旧预约、账单和执行结果不应继续假装有效。
- AA 分账支持手动改每个人要 A 的金额，Mock A 钱链接里的金额要同步变化。
- “其他问题”必须在页面内输入，不使用浏览器 prompt。
- 用户可见商户名不出现 `Mock 043` 这类测试编号，也不使用真实品牌名冒充真实商户。
- 宅家场景不把在线电影平台当成美团交易主推荐。
- Render 部署只用于演示，`LONGCAT_API_KEY` 可留空，所有交易动作仍然是 Mock。

## 常见问题回答

Q：为什么需要先追问？

A：周末规划里“时间、地点、预算、人数、品类”缺一个都可能导致推荐不可落地。先追问能避免把用户没说的默认值当成已确认条件。

Q：为什么要做朋友确认？

A：真实多人出行里，发起人不是唯一决策者。候选页内嵌共选，能把“朋友反馈”放在预约之前。

Q：这是一个真实交易系统吗？

A：不是。它是参赛 Demo 的本地 Mock 工作流，用来证明端到端状态和业务规则。
